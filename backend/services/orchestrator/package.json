{
  "name": "@claims/orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Claims Orchestrator - Step Functions-driven claim lifecycle (Intake -> Assessment -> Fraud_Check -> Payout/Disputed) with retry/backoff and escalation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@claims/shared": "0.1.0",
    "@aws-sdk/client-dynamodb": "3.1095.0",
    "@aws-sdk/lib-dynamodb": "3.1095.0"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "4.1.0"
  }
}
