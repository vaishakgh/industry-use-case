import { PORTAL_PACKAGE_NAME } from './index';

describe('@claims/portal placeholder', () => {
  it('exposes the package name constant', () => {
    expect(PORTAL_PACKAGE_NAME).toBe('@claims/portal');
  });
});
