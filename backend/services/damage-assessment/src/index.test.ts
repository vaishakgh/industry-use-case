import { DAMAGE_ASSESSMENT_PACKAGE_NAME } from './index';

describe('@claims/damage-assessment placeholder', () => {
  it('exposes the package name constant', () => {
    expect(DAMAGE_ASSESSMENT_PACKAGE_NAME).toBe('@claims/damage-assessment');
  });
});
