import { ORCHESTRATOR_PACKAGE_NAME } from './index';

describe('@claims/orchestrator placeholder', () => {
  it('exposes the package name constant', () => {
    expect(ORCHESTRATOR_PACKAGE_NAME).toBe('@claims/orchestrator');
  });
});
