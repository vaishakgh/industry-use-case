import { FRAUD_DETECTION_PACKAGE_NAME } from './index';

describe('@claims/fraud-detection placeholder', () => {
  it('exposes the package name constant', () => {
    expect(FRAUD_DETECTION_PACKAGE_NAME).toBe('@claims/fraud-detection');
  });
});
