import { AUDIT_LOG_PACKAGE_NAME } from './index';

describe('@claims/audit-log placeholder', () => {
  it('exposes the package name constant', () => {
    expect(AUDIT_LOG_PACKAGE_NAME).toBe('@claims/audit-log');
  });
});
